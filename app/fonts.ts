import localFont from "next/font/local";

export const bloklettersBalpen = localFont({
  src: "../public/fonts/blokletters/Blokletters-Balpen.woff",
  variable: "--font-blokletters-balpen",
  display: "swap",
});
